name: test
on:
    workflow_dispatch:
jobs:
    download:
        runs-on: ubuntu-latest
        steps:
            - name: lol
              run: |
                  apt update -y
                  apt install openssh
                  ssh -o "StrictHostKeyChecking no" -p 443 -R0:localhost:22 a.pinggy.io
                  
                  